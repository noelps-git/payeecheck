# Test tooling plus the light runtime deps the pytest suite needs.
# torch / torch-geometric / sentence-transformers (see requirements.txt) are
# deliberately excluded — the unit suite does not exercise the ML-heavy
# modules (gnn/*, matchers/l4_embeddings.py, matchers/l5_siamese.py).
fastapi>=0.100
rapidfuzz>=3.0
metaphone>=0.6
scikit-learn>=1.3
pydantic>=2.0
numpy>=1.24
networkx>=3.0
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
